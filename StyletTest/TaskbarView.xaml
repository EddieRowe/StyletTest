<UserControl x:Class="StyletTest.TaskbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:local="clr-namespace:StyletTest"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            
        </Grid.RowDefinitions>

        <!--
        <TextBlock Grid.Column="0" TextAlignment="Center" 
                   VerticalAlignment="Center" Text="{Binding TestBinding}"/>
-->
        <Button Grid.Column="1" Click="{s:Action DoClick}">Login</Button>
        <TextBlock Grid.Column="2" Margin="3" Text="{Binding TestBinding}"/> 
    </Grid>

    

    
</UserControl>
