<UserControl x:Class="StyletTest.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:local="clr-namespace:StyletTest"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Background="Beige">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!--Username-->
        <TextBox Grid.Row="1" Margin="20">Username</TextBox>

        <!--Password-->
        <TextBox Grid.Row="1" Grid.Column="2" Margin="20">****</TextBox>

        <!--Buttons-->
        <Button Grid.Row="2" Margin="70">Cancel</Button>
        <Button Grid.Row="2" Grid.Column="2" Margin="70">Login</Button>

    </Grid>
</UserControl>
